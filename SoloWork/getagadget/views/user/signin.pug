extends ../layout

block content
  section.user-cont
    h1 Sign in 
  
    if messages
      .errors
        for msg in messages
          .error 
            i.fa.fa-exclamation-triangle(aria-hidden='true')
            | #{msg}     
    
    form(action="/user/signin" method="post")
      label(for="email") Email
      input(type="email", name="email")#email
      label(for="password") password
      input(type="password", name="password")#password
      input(type="hidden", name="_csrf" value=`${crfToken}`)
      input(type="submit", value="sign in").button

    .msg Don't have an account? #[a.someClass(href="/user/signup") sign up] here!
    .msg or use #[a(href="/user/login-facebook") Facebook login]
    
